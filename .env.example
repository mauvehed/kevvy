# Discord Bot Token (Required)
# You can get this from the Discord Developer Portal
DISCORD_TOKEN=YOUR_DISCORD_BOT_TOKEN_HERE

# NVD API Key (Optional)
# Request one from https://nvd.nist.gov/developers/request-an-api-key
# Provides higher request limits for the NVD API.
# NVD_API_KEY=

# VulnCheck API Token (Required for VulnCheck fallback)
# Get one from https://vulncheck.com/
# VULNCHECK_API_TOKEN=

# Discord Command Prefix (Optional)
# The prefix used for bot commands. Defaults to "!" if not set.
# DISCORD_COMMAND_PREFIX=! 

# CISA KEV Polling interval in seconds (Defaults to 3600 = 1 hour)
CISA_KEV_INTERVAL_SECONDS=3600

# --- Optional Logging ---
DISABLE_DISCORD_LOGGING=true
# Guild (Server) ID where the logging channel resides
# LOGGING_GUILD_ID=
# Channel ID within the guild to send logs to
# LOGGING_CHANNEL_ID=

# --- Kevvy Web Reporting (Optional) ---
# Base URL for the kevvy-web status reporting server (e.g., http://localhost:8080)
KEVVY_WEB_URL=

# Secret API key shared with the kevvy-web server for authentication
KEVVY_WEB_API_KEY=