name: Run Python Tests

on:
  push:
    branches: [ main ]
    # Optional: Only run if files in the 'kevvy' directory changed
    paths:
     - 'kevvy/**'
  pull_request:
    branches: [ main ]
    # Optional: Only run if files in the 'kevvy' directory changed
    paths:
     - 'kevvy/**'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Use the Python version matching your project
        python-version: ['3.12'] 

    steps:
    - name: Check out code
      uses: actions/checkout@v4

    # Install Poetry BEFORE setting up Python, as setup-python needs it for caching
    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        virtualenvs-create: true
        virtualenvs-in-project: true
        installer-parallel: true

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'poetry'
        cache-dependency-path: 'poetry.lock'

    - name: Install dependencies via Poetry (including dev)
      run: poetry install --no-interaction --with dev

    - name: Run tests with pytest
      run: poetry run pytest -v